# ╔═══════════════════════════════════════════════════════════════╗
# ║ Air-Gapped NOC Copilot - Python Dependencies                 ║
# ║ All packages must be available offline (pip wheel)           ║
# ╚═══════════════════════════════════════════════════════════════╝

# ── Core ML Framework ──
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# ── Data Processing ──
pandas>=2.0.0
pyarrow>=12.0.0

# ── Database Connectivity ──
psycopg2-binary>=2.9.0
psycopg[binary]>=3.1.0

# ── HTTP Client ──
requests>=2.31.0
urllib3>=2.0.0

# ── Prometheus Metrics ──
prometheus-client>=0.17.0

# ── Configuration ──
pyyaml>=6.0
python-dotenv>=1.0.0

# ── RAG / Embeddings (offline) ──
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── CLI and Utilities ──
click>=8.1.0
tqdm>=4.65.0

# ── Testing ──
pytest>=7.4.0
pytest-cov>=4.1.0
